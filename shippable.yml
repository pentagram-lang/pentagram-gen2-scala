language: scala

jdk:
  - openjdk9

scala:
  - 2.12.4

branches:
  only:
    - master

build:
  ci:
    - sbt update compileAll test fmtCheck

  cache: true
  cache_dir_list:
    - $HOME/.ivy2
    - $HOME/.sbt
