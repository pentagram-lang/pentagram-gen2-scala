language: scala

jdk:
  - openjdk9

scala:
  - 2.12.4

branches:
  only:
    - master

build:
  ci:
    - sbt update
    - sbt compileAll
    - sbt test
    - sbt fmtCheck

  cache: true
  cache_dir_list:
    - $HOME/.ivy2
    - $HOME/.sbt
